{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "_comment": "ConnectionStrings:ApiCallMonitorDb can be set (e.g. in appsettings.Development.json or user secrets) to override where the SQLite database file lives. When absent, Program.cs defaults to App_Data/apicallmonitor.db next to the app."
}
